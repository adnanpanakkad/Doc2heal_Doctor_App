import 'package:flutter/material.dart';

class AppoinmentDetailScreen extends StatelessWidget {
  const AppoinmentDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}